<h2>all-posts.handlebars</h2>

{{#each posts as |post|}}
    <div class="card">
        <h2><a href="/post/{{post.userId}}">{{post.title}}</a></h2>
        <p>{{post.createdAt}}</p>
        <p>{{post.body}}</p>
        <p>username: {{post.userId}}</p>
    </div>
{{/each}}

