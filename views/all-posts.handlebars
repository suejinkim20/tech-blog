<h2>all-posts.handlebars</h2>

{{#each posts as |post|}}
    <div class="card">
        <div class="card-header">
            <h2><a href="/post/{{post.userId}}">{{post.title}}</a></h2>
        </div>
        <div class="card-body">
            <p>by: {{post.user.username}}</p>
            <p>{{post.createdAt}}</p>
            <p>{{post.body}}</p>
        </div>
    </div>
{{/each}}