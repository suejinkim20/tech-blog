<h2>all-posts-admin.handlebars</h2>

{{#each posts as |post|}}
    <div class="card">
        <h2>{{post.title}}</h2>
        <p>{{post.body}}</p>
        <p>{{post.userId}}</p>
    </div>
{{/each}}

